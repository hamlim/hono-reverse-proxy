{
  "name": "hono-reverse-proxy",
  "private": true,
  "workspaces": [
    "./workspaces/*"
  ],
  "devDependencies": {
    "turbo": "^1.12.2"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build:cli": "turbo run build --filter=reverse-proxy"
  },
  "dependencies": {
    "global-agent": "^3.0.0"
  }
}
